<?xml version="1.0" ?>
<project basedir=".." default="buildbot">
  <!-- This nant script is used by the SharpSvn buildbot -->
  
  <if test="{property::exists('build.include')}">
    <include buildfile="${build.include}" />
  </if>
  
  <target name="buildbot">
  </target>

</project>
