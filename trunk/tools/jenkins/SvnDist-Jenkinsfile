pipeline {
  agent any
  
  stages {
    stage('Build') {
      steps {
        bat """
          set
          nant -f imports/svn-dist/Default.build"
          @set
        """
      }
    }
  
  
  }
}